---
title: "Go 从入门到精通 · 第17篇：测试与性能分析"
date: 2025-01-09
weight: 1217
draft: false
tags: ["go"]
---

## 一、单元测试

### 测试文件命名

```
xxx_test.go
```

测试文件与被测文件放在同一包中。

### 基础测试

```go
// math.go
package math

func Add(a, b int) int {
    return a + b
}

func Divide(a, b int) (int, error) {
    if b == 0 {
        return 0, fmt.Errorf("除数不能为 0")
    }
    return a / b, nil
}
```

```go
// math_test.go
package math

import "testing"

func TestAdd(t *testing.T) {
    result := Add(2, 3)
    expected := 5
    if result != expected {
        t.Errorf("Add(2,3) = %d; want %d", result, expected)
    }
}

func TestDivide(t *testing.T) {
    result, err := Divide(10, 2)
    if err != nil {
        t.Fatal("不应报错:", err)
    }
    if result != 5 {
        t.Errorf("Divide(10,2) = %d; want 5", result)
    }
}

func TestDivideByZero(t *testing.T) {
    _, err := Divide(10, 0)
    if err == nil {
        t.Error("除数为 0 应该报错")
    }
}
```

```bash
# 运行测试
go test                     # 当前包
go test ./...               # 所有包
go test -v                  # 详细输出
go test -run TestAdd        # 运行指定测试
go test -count=1            # 禁用缓存
```

### Table-Driven Test

```go
func TestAddTable(t *testing.T) {
    tests := []struct {
        name     string
        a, b     int
        expected int
    }{
        {"正数相加", 2, 3, 5},
        {"负数相加", -1, -2, -3},
        {"零值相加", 0, 0, 0},
        {"正负相加", 5, -3, 2},
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            result := Add(tt.a, tt.b)
            if result != tt.expected {
                t.Errorf("Add(%d,%d) = %d; want %d",
                    tt.a, tt.b, result, tt.expected)
            }
        })
    }
}
```

### Setup 和 Teardown

```go
func TestMain(m *testing.M) {
    fmt.Println("=== 所有测试开始前 ===")
    
    // 初始化数据库、创建文件等
    setup()
    
    code := m.Run()  // 执行所有测试
    
    // 清理资源
    teardown()
    
    fmt.Println("=== 所有测试结束后 ===")
    os.Exit(code)
}
```

---

## 二、子测试

```go
func TestUser(t *testing.T) {
    // 并行子测试
    t.Run("创建用户", func(t *testing.T) {
        t.Parallel()
        // 测试...
    })
    
    t.Run("查询用户", func(t *testing.T) {
        t.Parallel()
        // 测试...
    })
}
```

---

## 三、测试辅助函数

### 使用 helper

```go
func assertEqual(t *testing.T, got, want interface{}, msg ...string) {
    t.Helper()  // 标记为辅助函数，出错时报告调用者的行号
    if got != want {
        defaultMsg := fmt.Sprintf("got %v, want %v", got, want)
        if len(msg) > 0 {
            defaultMsg = msg[0] + ": " + defaultMsg
        }
        t.Error(defaultMsg)
    }
}

func TestAdd(t *testing.T) {
    assertEqual(t, Add(2, 3), 5)
    assertEqual(t, Add(-1, 1), 0, "正负数相加")
}
```

### 临时目录

```go
func TestFile(t *testing.T) {
    dir := t.TempDir()  // 自动清理
    path := filepath.Join(dir, "test.txt")
    
    os.WriteFile(path, []byte("hello"), 0644)
    data, _ := os.ReadFile(path)
    
    if string(data) != "hello" {
        t.Error("内容不匹配")
    }
}
```

---

## 四、Mock 和接口

```go
// 依赖接口定义
type EmailSender interface {
    Send(to, subject, body string) error
}

type UserService struct {
    sender EmailSender
}

func (s *UserService) Register(name, email string) error {
    // 注册逻辑...
    return s.sender.Send(email, "欢迎注册", "欢迎,"+name)
}

// 测试时用 mock
type mockSender struct {
    calls []string
}

func (m *mockSender) Send(to, subject, body string) error {
    m.calls = append(m.calls, to)
    return nil
}

func TestRegister(t *testing.T) {
    mock := &mockSender{}
    svc := &UserService{sender: mock}
    
    svc.Register("张三", "zs@test.com")
    
    if len(mock.calls) != 1 {
        t.Error("应该发送一封邮件")
    }
}
```

---

## 五、基准测试

```go
func BenchmarkAdd(b *testing.B) {
    // b.N 由框架决定
    for i := 0; i < b.N; i++ {
        Add(2, 3)
    }
}

func BenchmarkStringConcat(b *testing.B) {
    // 重置计时器
    b.ResetTimer()
    
    for i := 0; i < b.N; i++ {
        var s string
        for j := 0; j < 100; j++ {
            s += "a"
        }
    }
}

func BenchmarkStringBuilder(b *testing.B) {
    b.ResetTimer()
    
    for i := 0; i < b.N; i++ {
        var sb strings.Builder
        for j := 0; j < 100; j++ {
            sb.WriteString("a")
        }
        _ = sb.String()
    }
}
```

```bash
# 运行基准测试
go test -bench=. -benchmem
# BenchmarkAdd-8               1000000000  0.25 ns/op   0 B/op  0 allocs/op
# BenchmarkStringConcat-8         100000  20000 ns/op  5000 B/op  100 allocs/op
# BenchmarkStringBuilder-8       1000000   1000 ns/op   500 B/op  1 allocs/op
```

---

## 六、性能分析（pprof）

```go
import (
    "net/http"
    _ "net/http/pprof"  // 导入 pprof
)

func main() {
    go func() {
        log.Println(http.ListenAndServe("localhost:6060", nil))
    }()
    
    // 应用逻辑...
}
```

```bash
# 分析 CPU
go tool pprof http://localhost:6060/debug/pprof/profile?seconds=30

# 分析内存
go tool pprof http://localhost:6060/debug/pprof/heap

# 分析协程
go tool pprof http://localhost:6060/debug/pprof/goroutine

# 生成火焰图
go tool pprof -http=:8080 ~/pprof/pprof.samples.cpu.001.pb.gz
```

---

## 七、覆盖率

```bash
# 查看测试覆盖率
go test -cover
# ok      example/math   0.002s   coverage: 80.0% of statements

# 生成覆盖率报告
go test -coverprofile=coverage.out
go tool cover -html=coverage.out  # 浏览器查看
```

---

## 小结

测试是 Go 文化的一部分。Table-driven test 是标准模式，testing 包提供了测试、基准测试、子测试等完整功能。pprof 是性能分析的利器，配合火焰图可以快速定位性能瓶颈。

---

