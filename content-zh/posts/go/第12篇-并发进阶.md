---
title: "Go 从入门到精通 · 第12篇：并发进阶"
date: 2025-01-04
weight: 1212
draft: false
tags: ["go"]
---

## 一、select

select 让一个 goroutine 同时等待多个 channel 操作。

### 基本用法

```go
func main() {
    ch1 := make(chan string)
    ch2 := make(chan string)

    go func() {
        time.Sleep(1 * time.Second)
        ch1 <- "来自 ch1"
    }()

    go func() {
        time.Sleep(2 * time.Second)
        ch2 <- "来自 ch2"
    }()

    select {
    case msg := <-ch1:
        fmt.Println(msg)
    case msg := <-ch2:
        fmt.Println(msg)
    case <-time.After(3 * time.Second):
        fmt.Println("超时")
    default:
        fmt.Println("没有准备好的 channel")
    }
}
```

### select 规则

```
1. 从已关闭的 channel 接收，立即返回零值（不是阻塞）
2. 向 nil channel 发送/接收，永远阻塞
3. 多个 case 同时满足，随机选择一个
4. 所有 channel 都阻塞，走 default（如果有）
5. 所有 channel 都阻塞且没有 default，阻塞等待
```

### 超时控制

```go
// 方式1：time.After
select {
case result := <-ch:
    fmt.Println(result)
case <-time.After(2 * time.Second):
    fmt.Println("操作超时")
}

// 方式2：context 超时（推荐，更灵活）
func doWithTimeout(ctx context.Context, ch chan int) error {
    select {
    case result := <-ch:
        fmt.Println(result)
        return nil
    case <-ctx.Done():
        return ctx.Err()
    }
}
```

### 非阻塞通信

```go
// 非阻塞发送
select {
case ch <- value:
    // 发送成功
default:
    // 发送失败（缓冲区满），不阻塞
}

// 非阻塞接收
select {
case v := <-ch:
    fmt.Println("收到:", v)
default:
    fmt.Println("没有数据")
}
```

### 扇出模式

```go
// 同一个数据发到多个 channel
func fanOut(ch <-chan int, outs []chan<- int) {
    for v := range ch {
        for _, out := range outs {
            out <- v
        }
    }
}
```

---

## 二、sync 包

### sync.WaitGroup

```go
func main() {
    var wg sync.WaitGroup

    for i := 0; i < 5; i++ {
        wg.Add(1)
        go func(id int) {
            defer wg.Done()
            fmt.Printf("Worker %d 开始\n", id)
            time.Sleep(time.Second)
            fmt.Printf("Worker %d 完成\n", id)
        }(i)
    }

    wg.Wait()  // 等待所有 worker
    fmt.Println("全部完成")
}
```

### sync.Mutex

```go
type Counter struct {
    mu    sync.Mutex
    value int
}

func (c *Counter) Inc() {
    c.mu.Lock()
    defer c.mu.Unlock()
    c.value++
}

func (c *Counter) Value() int {
    c.mu.Lock()
    defer c.mu.Unlock()
    return c.value
}
```

### sync.RWMutex

```go
type Cache struct {
    mu    sync.RWMutex
    data  map[string]string
}

func (c *Cache) Get(key string) string {
    c.mu.RLock()        // 读锁，允许并发读
    defer c.mu.RUnlock()
    return c.data[key]
}

func (c *Cache) Set(key, value string) {
    c.mu.Lock()         // 写锁，互斥
    defer c.mu.Unlock()
    c.data[key] = value
}
```

### sync.Once

```go
var once sync.Once
var config *Config

func GetConfig() *Config {
    once.Do(func() {
        fmt.Println("初始化配置（只执行一次）")
        config = loadConfig()
    })
    return config
}
```

### sync.Pool

```go
var pool = sync.Pool{
    New: func() interface{} {
        return &bytes.Buffer{}
    },
}

func process() {
    buf := pool.Get().(*bytes.Buffer)
    buf.Reset()
    defer pool.Put(buf)

    // 使用 buf
}
// 用完归还，下次复用，减少内存分配
```

### sync.Map

```go
// 并发安全的 map（适合读多写少、key 不冲突的场景）
var m sync.Map

// 写
m.Store("key", "value")

// 读
v, ok := m.Load("key")

// 删除
m.Delete("key")

// 遍历
m.Range(func(key, value interface{}) bool {
    fmt.Println(key, value)
    return true  // 继续遍历
})
```

---

## 三、Context 包

context 用于传递取消信号和请求范围的值。

### 创建 Context

```go
// 根 context
ctx := context.Background()
ctx := context.TODO()

// 带取消
ctx, cancel := context.WithCancel(context.Background())
defer cancel()  // 通常 defer 调用

// 带超时
ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
defer cancel()

// 带截止时间
ctx, cancel := context.WithDeadline(context.Background(), time.Now().Add(2*time.Second))
defer cancel()

// 附加值
ctx = context.WithValue(ctx, "user_id", 123)
```

### 实战：超时取消

```go
func fetchData(ctx context.Context, url string) ([]byte, error) {
    req, err := http.NewRequestWithContext(ctx, "GET", url, nil)
    if err != nil {
        return nil, err
    }

    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        return nil, err
    }
    defer resp.Body.Close()

    return io.ReadAll(resp.Body)
}

func main() {
    ctx, cancel := context.WithTimeout(context.Background(), 3*time.Second)
    defer cancel()

    data, err := fetchData(ctx, "https://api.example.com/data")
    if err != nil {
        fmt.Println("请求失败:", err)
        return
    }
    fmt.Println(string(data))
}
```

### 级联取消

```go
func main() {
    ctx, cancel := context.WithCancel(context.Background())

    go func() {
        time.Sleep(2 * time.Second)
        cancel()  // 取消所有子任务
    }()

    var wg sync.WaitGroup
    for i := 0; i < 3; i++ {
        wg.Add(1)
        go func(id int) {
            defer wg.Done()
            select {
            case <-time.After(5 * time.Second):
                fmt.Printf("任务 %d 完成\n", id)
            case <-ctx.Done():
                fmt.Printf("任务 %d 被取消\n", id)
            }
        }(i)
    }

    wg.Wait()
    fmt.Println("主程序结束")
}
```

---

## 四、并发模式

### 1. Pipeline（流水线）

```go
func gen(nums ...int) <-chan int {
    out := make(chan int)
    go func() {
        for _, n := range nums {
            out <- n
        }
        close(out)
    }()
    return out
}

func square(in <-chan int) <-chan int {
    out := make(chan int)
    go func() {
        for n := range in {
            out <- n * n
        }
        close(out)
    }()
    return out
}

func main() {
    // 数据流：1,2,3,4 → 平方 → 输出
    nums := gen(1, 2, 3, 4)
    squares := square(nums)

    for v := range squares {
        fmt.Println(v)  // 1, 4, 9, 16
    }
}
```

### 2. Fan-Out / Fan-In

```go
// Fan-Out：将数据分发到多个 worker
func fanOut(in <-chan int, workers int) []<-chan int {
    channels := make([]<-chan int, workers)
    for i := 0; i < workers; i++ {
        channels[i] = worker(in)
    }
    return channels
}

// Fan-In：合并多个 channel
func fanIn(chs ...<-chan int) <-chan int {
    out := make(chan int)
    var wg sync.WaitGroup

    for _, ch := range chs {
        wg.Add(1)
        go func(c <-chan int) {
            defer wg.Done()
            for v := range c {
                out <- v
            }
        }(ch)
    }

    go func() {
        wg.Wait()
        close(out)
    }()

    return out
}
```

### 3. 超时 + 重试

```go
func fetchWithRetry(ctx context.Context, url string, retries int) ([]byte, error) {
    for i := 0; i < retries; i++ {
        select {
        case <-ctx.Done():
            return nil, ctx.Err()
        default:
        }

        data, err := fetchData(ctx, url)
        if err == nil {
            return data, nil
        }

        // 指数退避
        time.Sleep(time.Duration(1<<uint(i)) * 100 * time.Millisecond)
    }
    return nil, fmt.Errorf("重试 %d 次后失败", retries)
}
```

### 4. 并发控制（信号量）

```go
func main() {
    sem := make(chan struct{}, 3)  // 最多 3 个并发
    var wg sync.WaitGroup

    for i := 0; i < 10; i++ {
        wg.Add(1)
        go func(id int) {
            defer wg.Done()

            sem <- struct{}{}      // 获取令牌
            defer func() { <-sem }() // 释放令牌

            fmt.Printf("任务 %d 开始\n", id)
            time.Sleep(time.Second)
            fmt.Printf("任务 %d 完成\n", id)
        }(i)
    }

    wg.Wait()
}
```

---

## 五、竞态检测

```go
// 编译/运行时加上 -race 检测数据竞争
// go run -race main.go
// go build -race ./...

// hello.go
func main() {
    var count int
    var wg sync.WaitGroup

    for i := 0; i < 1000; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            count++  // 数据竞争！
        }()
    }

    wg.Wait()
    fmt.Println(count)
}
```

```bash
go run -race main.go
# 输出会显示 WARNING: DATA RACE
```

**修复竞态**：用 Mutex、Atomic 或 channel 同步。

---

## 小结

这一篇学习了 select、sync 包、Context、并发模式和竞态检测。掌握这些工具后，你能写出安全、高效的并发程序。Goroutine 是工具，channel 是通信方式，但工具要组合成模式（Pipeline、Fan-Out/In）才能解决实际问题。

---

